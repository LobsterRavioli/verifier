import { Id, SchemaHash } from '@iden3/js-iden3-core';
import { DocumentLoader } from '@iden3/js-jsonld-merklization';
import { VerifyOpts } from './registry';
export interface Query {
    allowedIssuers: string[];
    credentialSubject: {
        [key: string]: unknown;
    };
    context: string;
    type: string;
    claimID?: string;
    skipClaimRevocationCheck?: boolean;
}
export interface ClaimOutputs {
    issuerId: Id;
    schemaHash: SchemaHash;
    slotIndex?: number;
    operator: number;
    value: bigint[];
    timestamp: number;
    merklized: number;
    claimPathKey?: bigint;
    claimPathNotExists?: number;
    valueArraySize: number;
    isRevocationChecked: number;
}
export declare function checkQueryRequest(query: Query, outputs: ClaimOutputs, schemaLoader?: DocumentLoader, verifiablePresentation?: JSON, opts?: VerifyOpts): Promise<void>;
